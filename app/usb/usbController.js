/*(function() {
  angular.module('app')
    .controller('usbController', ['$scope', '$q',
      USBController
    ]).config(function($mdIconProvider) {
      $mdIconProvider.iconSet("avatar",
        'assets/icons/avatar-icons.svg',
        128);
    });
  //后期将diskController.js中关于USB的逻辑进行拆分
  function USBController($scope, $q) {
    var self = this;


  }
})();
*/
